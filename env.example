# ─────────────────────────────────────────────────────────────────────────────
# ProductoraClips — Variables de entorno
# Copia este fichero como .env y rellena los valores reales.
# NUNCA subas .env a git (ya está en .gitignore).
# ─────────────────────────────────────────────────────────────────────────────

# ── Vmake API (Proyecto 9 — Eliminación de Grafismos) ─────────────────────────
# Obtén tus credenciales en: https://vmake.ai/developer
MT_AK=tu_access_key_aqui
MT_SK=tu_secret_key_aqui

# ── Flask (opcional) ──────────────────────────────────────────────────────────
# FLASK_DEBUG=1   # activa debug mode (no usar en producción)
